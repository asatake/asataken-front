import Layout from "../components/layout";
import { Container } from "react-bootstrap";

function Work() {
    return (
        <Layout>
            <Container className="mt-4">
                <h1>WORK</h1>
                <p>準備中</p>
            </Container>
        </Layout>
    );
}

export default Work;
